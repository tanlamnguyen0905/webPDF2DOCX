# Diagnostic Log: Tạo UI/UX Design cho Future Features

Date: 2026-06-28
Working Directory: C:\Users\tanbe\OneDrive\Documents\ConvertPDF2DOCX\pdf-to-word-converter

## Initial Request
User yêu cầu đọc `done/TechSpec/task_future.md` và tạo `done/UI_UX_Design/ui_des_future.md` mô tả chi tiết UI/UX cho toàn bộ chức năng MVP. Yêu cầu tham khảo `ui_des_now.md` để đồng bộ.

## Investigation Steps
1. Đã đọc `done/TechSpec/task_future.md` - Xác định tất cả future task: Thanh toán (MoMo/VNPAY), OCR, Batch, Queue, Chatbot AI, Subscription, API bên thứ ba, Dashboard nâng cao, Bảo mật nâng cao (24 task).
2. Đã đọc `done/UI_UX_Design/ui_des_now.md` - Nắm cấu trúc template, design tokens, route mapping, cách mô tả từng màn hình (mục đích, component, dữ liệu, hành động, trạng thái, validation, responsive, route).

## Conclusion
Cần tạo tài liệu UI/UX song song với `ui_des_now.md` nhưng dành cho các tính năng Post-MVP. Giữ nguyên cấu trúc và naming convention của tài liệu gốc.

## Proposed Fix / Resolution
Đã tạo file `done/UI_UX_Design/ui_des_future.md` với 17 sections:
- 7 sections tương ứng 7 nhóm task trong task_future.md
- Mở rộng: Layout, Responsive, States, Route Mapping (25 routes mới)
- Design tokens mở rộng (status colors, shadows, z-index, border radius)
- Accessibility, i18n preparation, Permission-based UI matrix, Glossary
- Implementation checklist theo 4 phases (Phase 2-5)

## Notes
- Tái sử dụng component chung từ thiết kế MVP
- Cập nhật header navigation, sidebar mobile, user dropdown
- Tất cả màn hình đều có đủ 6 phần: mục đích, component, dữ liệu, hành động, trạng thái, responsive

---
# Diagnostic Log: Cập nhật Schema Database sau Review

## Initial Request
User yêu cầu cập nhật `done/TechSpec/schema.md` dựa trên các đề xuất từ review trước đó.

## Investigation Steps
1. Đã có sẵn review với đầy đủ danh sách thiếu sót: 9 bảng mới, 8 cột mới, index bổ sung
2. Tham chiếu lại `task_future.md`, `ui_des_future.md`, `requires.md`, `tech_arch.md`, `analytic_system.md` để đảm bảo không bỏ sót

## Conclusion
Cần cập nhật schema.md toàn diện bao gồm: mở rộng bảng hiện tại, thêm bảng mới, cập nhật DDL, index, seed data, và các section liên quan.

## Resolution
Đã thực hiện các thay đổi sau:
1. **Tổng quan thiết kế**: Thêm 4 nhóm dữ liệu mới (Subscription, Batch & API, Notification & Email, Bảo mật)
2. **ERD**: Cập nhật mermaid với 8 quan hệ mới cho subscription, batch, api_keys, notifications, virus_scans
3. **Bảng**: Mở rộng từ 13 → 22 bảng, thêm cột giai đoạn (MVP/Phase 3-5)
4. **Bảng users**: Thêm 4 cột: `avatar_url`, `subscription_tier`, `last_login_ip`, `deleted_at`
5. **Bảng refresh_tokens**: Thêm mới cho JWT refresh token management
6. **Bảng payments**: Thêm `subscription_id` FK
7. **Bảng conversion_jobs**: Thêm 5 cột: `source`, `conversion_settings` (JSON), `batch_job_id`, `retry_count`
8. **Bảng system_settings**: Đổi `setting_value` VARCHAR(500) → TEXT
9. **Bảng chatbot_messages**: Thêm `metadata` (JSON) cho LLM context
10. **8 bảng mới**: subscription_plans, user_subscriptions, batch_jobs, api_keys, webhooks, notifications, email_logs, virus_scans
11. **SQL DDL**: Cập nhật CREATE TABLE cho bảng cũ + thêm đầy đủ DDL cho 8 bảng mới
12. **Index**: Thêm 12 index mới cho các bảng mới + `support_messages_is_read`
13. **Seed data**: Thêm 6 system settings mới (OCR, rate limit, virus scan)
14. **Section 9 (Quan hệ)**: Thêm 3 quan hệ mới (subscription, batch, notification)
15. **Section 12 (Bảng MVP)**: Cập nhật danh sách + thêm `refresh_tokens`
16. **Section 13 (Migration order)**: Chia làm 2 nhóm: MVP (12 bảng) + Future (10 bảng theo phase)

## Modified Files
- `Modified` - `done/TechSpec/schema.md`

---
# Diagnostic Log: Đồng bộ schema.md với backend migration

## Initial Request
User yêu cầu cập nhật đồng bộ backend migration V1 đã có với schema.md đã review.

## Investigation Steps
1. Đọc `backend/src/main/resources/db/migration/V1__init_schema.sql` — Phát hiện 2 chiều cần đồng bộ
2. Đọc `V2__seed_data.sql` — Phát hiện thiếu 6 config mới

## Changes Made

### V1__init_schema.sql (MVP backend)
- **users**: Thêm `avatar_url`, `subscription_tier`, `last_login_ip`, `deleted_at`
- **Thêm bảng** `refresh_tokens` (tách giữa password_reset_tokens và coin_packages)
- **conversion_jobs**: Thêm `source`, `conversion_settings` (JSON), `retry_count`
- **system_settings**: `setting_value` VARCHAR(500) → TEXT
- **chatbot_messages**: Thêm `metadata` (JSON)
- **Index**: Thêm `idx_support_messages_is_read`

### V2__seed_data.sql
- Thêm 6 config: max_file_size_premium_mb, max_pages_premium, file_retention_original_days, rate_limit_upload_per_minute, rate_limit_chatbot_per_minute, virus_scan_enabled

### schema.md (đồng bộ ngược)
- `conversion_jobs.status` thêm `QUEUED` (V1 đã có)

### Không thêm vào V1 (sẽ là migration riêng theo phase)
- `payments.subscription_id` — FK lỗi nếu chưa có `user_subscriptions`
- `conversion_jobs.batch_job_id` — FK lỗi nếu chưa có `batch_jobs`

## Conclusion
Đã đồng bộ hoàn chỉnh 2 chiều giữa schema.md và backend migration files.

---
# Diagnostic Log: Review api_spec.md đối chiếu UI/UX Design

## Initial Request
User yêu cầu review `done/backend/api_spec.md` dựa trên thông tin từ `done/UI_UX_Design/`.

## Investigation Steps
1. Đọc `ui_des_now.md` — 17 routes MVP, các component, hành động gọi API
2. Đọc `ui_des_future.md` — 25 routes future, các tính năng Phase 2-5
3. Đọc `api_spec.md` — 25 sections, 21 API checklist MVP, đối chiếu từng endpoint

## Findings

### Critical (20+ endpoint thiếu)
- **User**: Avatar upload (`POST /users/me/avatar`)
- **Conversion**: Cancel job (`POST /conversions/{id}/cancel`)
- **Chatbot**: SSE streaming response
- **Payment**: MoMo/VNPAY endpoints, Payment result polling
- **Batch**: CRUD batches + download ZIP
- **Subscription**: Plans CRUD, register, manage, cancel
- **API Keys**: CRUD + status management
- **Webhooks**: CRUD webhook URLs
- **Notifications**: List, read, read-all
- **Admin**: Monitoring, Rate limits, Virus scan, Support agents
- **Realtime**: SSE/WebSocket cho conversion status

### Major (15+ DTO/Enum thiếu)
- **UserDto**: thiếu `avatarUrl`, `subscriptionTier`
- **ConversionJobDto**: thiếu `source`, `conversionSettings`, `retryCount`
- **PaymentDto**: thiếu `subscriptionId`
- **UploadPreviewDto**: thiếu `virusScanStatus`, OCR gợi ý
- **Enum mới**: Subscription, Batch, ApiKey, Webhook, Notification, VirusScan (8 enum)

### Minor (Route mapping)
- Section 20 chưa map 8 routes future

## Conclusion
api_spec.md hiện tại đầy đủ cho MVP (21 API), nhưng thiếu ~20 endpoint cho future features và 15+ DTO/Enum cần cập nhật theo schema mới.

---
# Diagnostic Log: Cập nhật api_spec.md hoàn chỉnh

## Initial Request
User yêu cầu update api_spec.md dựa trên review.

## Changes Made
1. **Phiên bản**: 1.0 MVP → 2.0 (đồng bộ schema.md v2.0)
2. **Enum mới**: Subscription (3.6), Batch (3.7), API Key/Webhook (3.8), Notification (3.9), Virus Scan (3.10)
3. **DTO mới**: UserDto (avatarUrl, subscriptionTier), ConversionJobDto (source, conversionSettings, queuePosition, retryCount), PaymentDto (subscriptionId, paymentUrl), UploadPreviewDto (scanAnalysis, virusScan)
4. **5 DTOs mới**: SubscriptionPlanDto, UserSubscriptionDto, BatchJobDto, ApiKeyDto, WebhookDto, NotificationDto (4.10-4.15)
5. **Endpoint mới**: Avatar upload (5.8), Cancel conversion (6.10), MoMo payment (8.6-8.7), VNPAY (8.8-8.9)
6. **6 sections mới**: Subscription (9), Batch (10), API Keys (11), Webhooks (12), Notifications (13)
7. **Section renumber**: 25 → 30 sections
8. **SSE realtime** (24.2): cho realtime conversion status
9. **Route mapping** (25): thêm 17 routes future
10. **MVP checklist** (26): mở rộng từ 21 → 40+ API endpoints
11. **Service list** (27): thêm 5 services (Subscription, Batch, Notification, ApiKey, Webhook)
12. **Env variables** (28): thêm MoMo, VNPAY, Chatbot LLM, feature flags