# Diagnostic Log: Tạo cấu trúc thư mục monorepo cho dự án Convert PDF to Word

Date: 2026-06-22
Working Directory: C:\Users\tanbe\OneDrive\Documents\ConvertPDF2DOCX\pdf-to-word-converter

## Initial Request
Người dùng yêu cầu đọc thư mục `done/` để hiểu dự án, sau đó tạo cấu trúc thư mục phù hợp.

## Investigation Steps
1. Đọc toàn bộ tài liệu trong `done/`:
   - `TechSpec/requires.md` — SRS (yêu cầu hệ thống, coin, nạp tiền, MVP).
   - `TechSpec/analystic_system.md` — phân tích hệ thống, vai trò, luồng.
   - `TechSpec/schema.md` — thiết kế DB (13 bảng, DDL MySQL 8, index, seed).
   - `TechSpec/tech_arch.md` — kiến trúc: Next.js + Spring Boot + MySQL + LocalStack S3/SQS + Python Worker.
   - `backend/api_spec.md` — đặc tả REST API đầy đủ (auth, conversion, coin, payment, support, admin, internal worker, SQS message).
   - `UI_UX_Design/ui_des.md` & `ui_flow.md` — UI/UX, route Next.js, component, layout theo vai trò.
2. Hỏi người dùng làm rõ phạm vi → chọn: **Scaffold + file cấu hình mẫu** cho **cả 3 service + docker**.
3. Phát hiện mâu thuẫn: `README.md` cũ mô tả stack FastAPI + Celery + Redis + PostgreSQL, trái với `done/` (Spring Boot + SQS + MySQL). Đã hỏi lại → người dùng chốt **theo done/ (Spring Boot + SQS)**.

## Conclusion
Dựng monorepo 3 service theo đúng tài liệu `done/`:
- `frontend/` Next.js (App Router) — route group (public)/(auth)/(user)/admin/support-dashboard, component, lib.
- `backend/` Spring Boot (Java 17) — package-by-feature, Flyway migration theo schema.md, config S3/SQS/JWT.
- `worker/` Python — poll SQS, convert PDF→DOCX, callback backend.
- `infra/` + `docker-compose.yml` — MySQL + LocalStack (S3, SQS).

## Proposed Fix / Resolution
- Tạo cây thư mục đầy đủ (mkdir) cho 3 service + infra.
- Ghi file cấu hình mẫu (KHÔNG chạy init/cài dependency):
  - Root: `README.md` (cập nhật lại khớp stack), `.gitignore`, `.env.example`, `docker-compose.yml`.
  - Infra: script init LocalStack (S3 bucket + SQS queue + DLQ), MySQL init SQL.
  - Frontend: `package.json`, `tsconfig.json`, `next.config.mjs`, Tailwind/PostCSS/ESLint, Dockerfile, layout/page placeholder cho toàn bộ route, component stub, lib (api client, routes, types, utils).
  - Backend: `pom.xml`, `application.yml`(+local), Flyway `V1__init_schema.sql` + `V2__seed_data.sql`, main app, common (ApiResponse/Page/Exception/ErrorCode), config (Security/AWS), security (JwtTokenProvider), domain `conversion` đầy đủ làm mẫu + `package-info.java` cho các domain khác, storage/queue/internal stub, Dockerfile, README.
  - Worker: `requirements.txt`, `app/` (main, config, aws_clients, callback_client, converter/pdf_to_docx + ocr), Dockerfile, README, tests.
- Ghi đè `README.md` và `docker-compose.yml` cũ (FastAPI/Celery) sau khi người dùng xác nhận theo `done/`.
- Tổng cộng ~137 file. Đã xóa thư mục thừa `backend/src/main/test` tạo nhầm.

## Notes
- Đây là bản **scaffold**: nhiều file là placeholder/stub kèm `// TODO` trỏ tới mục tương ứng trong `done/`. Cần chạy `npm install`, tạo Maven wrapper, `pip install` trước khi chạy thật.
- Còn tồn tại `.github/workflows` và `.vscode` từ trước — chưa rà soát, có thể còn cấu hình theo stack cũ.
- Backend dùng `ddl-auto=validate` → schema do Flyway quản lý.

## Modified Files
- `[Created]` - nhiều file trong `frontend/`, `backend/`, `worker/`, `infra/` (~137 file)
- `[Modified]` - `README.md`, `.gitignore`, `docker-compose.yml` (ghi đè bản FastAPI/Celery cũ)
- `[Created]` - `.env.example` (root)
- `[Deleted]` - `backend/src/main/test/` (thư mục rỗng tạo nhầm)
